{
  "_comment_about_this_file": [
    "Hand-editable BLB Stage-2 action JSON template (full slots form).",
    "This is the 'copy your RL training run's blb_stage2_best_action_full.json",
    "and tweak it line-by-line' starting point.",
    "Each entry in 'slots' is a per-action-slot row; the loader rebuilds the",
    "raw int action_vec by snapping each scaling_factor / truncation_bits to",
    "its nearest selectable level (audit warnings are emitted if a snap",
    "happened).",
    "Load via:",
    "  bash run_search.sh eval --preset mrpc-final-eval-only \\",
    "    --action-config configs/evaluation/actions/manual_blb_v3_full_slots_template.json",
    "To get a real starting point with all 877 slots already filled in,",
    "copy the file written by your latest RL run:",
    "  <persistent_run>/stage2_noise/progress/blb_stage2_best_action_full.json",
    "then prune to just the slots you want to change.",
    "Schema docs: see Paean/action_grid.py::load_action_grid_config and",
    "             blb_stage2_rl/action_io.py::slots_payload_to_action_vec."
  ],
  "schema_version": "blb_v3_slots_human_v1",
  "label": "manual_template",
  "profile": "mrpc",
  "num_layers": 12,
  "gelu_degree": [4, 4, 4, 4, 4, 4, 4, 4, 4, 4, 4, 4],
  "attn_degree": [4, 4, 4, 4, 4, 4, 4, 4, 4, 4, 4, 4],
  "_minimal_slot_example": {
    "L3.B5.W.wffn1": {"scaling_factor": 22},
    "L3.B5.K":       {"truncation_bits": 11},
    "L7.B2.R.gamma_r": {"scaling_factor": null}
  },
  "slots": {}
}
